<% @page_title = "Delete Subjects" %>

<div>

  <%= form_for(@subject, :method => 'delete') do |f| %>

    <h2>Delete This Subject : <%= @subject.name %> </h2>

    <div class="form-buttons">
      <%= f.submit("Delete Subject") %>
    </div>

  <% end %>
</div>
